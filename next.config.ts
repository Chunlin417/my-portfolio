import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // This project currently ships as a static portfolio site with no API
  // routes, server actions, or auth. If you deploy to a static-only host
  // (e.g. GitHub Pages, Netlify without SSR), uncomment the line below:
  // output: "export",
};

export default nextConfig;
